name: Ping Test

on:
  schedule:
    - cron: "0,10,20,30,40,50 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping
        run: echo "$(date -u +%Y-%m-%dT%H:%M:%SZ) ping"
