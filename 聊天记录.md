嗨 Xiaoyu, 您正在与 Fin AI Agent 交谈。为了帮助我提供最佳答案，请尽可能详细地分享您的问题。
Fin • AI Agent • 6分钟
我想要通过 API 来监控每小时会话 tag 的某一类会话 tag 的数量，这可能吗？
请你为我列举解决方案。
Xiaoyu • 6分钟
可以的，Xiaoyu。你可以用 API 实现“每小时监控某一类会话 tag 的数量”，但需要你自己做时间分段统计，因为 API 本身不直接提供“按小时聚合的 tag 数量”指标。
两种可行方案：
方案一（推荐）：用“Build your own reports” API 按 tag_id 过滤会话，然后在你的系统里按小时统计返回的会话数量。
方案二：用 /counts?type=user&count=tag 获取用户 tag 的总数，但这个是用户 tag 的总数量，不是会话 tag 的每小时数量，所以不适合你的场景。
如果你告诉我你想要监控的是“会话 tag”还是“用户 tag”，我可以帮你选更匹配的 API 路径。
Fin • AI Agent • 5分钟
是会话 tag，不是用户 tag
Xiaoyu • 3分钟
Xiaoyu，会话标签的“每小时数量”目前没有直接的 API 指标，但你可以用以下两种方式实现监控。
方案一（推荐）：使用“Build your own reports” API 按 tag_id 过滤会话，然后在你的系统里将返回的会话按小时分组统计数量。
示例：tag_ids 字段用 = 或 IN 过滤指定标签。
方案二（适合大批量）：使用 Reporting Data Export API 导出会话数据（包含 conversation_id 和 conversation_started_at），再按小时聚合统计。
这个是异步导出，需要轮询状态，下载链接会过期。
Fin • AI Agent • 3分钟
这就是您要找的内容吗？
Fin • AI Agent • 3分钟
我希望持续监听 会话 tag 标注量，动态的监听近 1h tag 为“**”时的数量达到**个就给我同步，我想知道是否有这样的监听时间及 api 可用。或者你给我一个放你。
Xiaoyu • 1分钟
Xiaoyu，目前没有“实时监听会话 tag 数量并达到阈值就立刻推送”的事件 API。
最接近的方案是“轮询 + 自己判断”：用 Build your own reports API 按 tag_ids 过滤会话，然后在你的系统里把返回的会话按“近 1 小时”分组统计数量，一旦达到你设定的数字就触发通知。
如果你更希望在界面里看趋势，也可以用“Conversation tags”报告里的“Tagged conversations - by time”来观察按时间分布的标注量。
Fin • AI Agent • 刚才

这是否解答了您的问题？

Xiaoyu，你提的 3 个前提里，目前我能确认的是第 1 点（API 能力边界），第 2 点（速率限制）和第 3 点（去重逻辑）在文档里没有说明。
Search Conversations API 的 created_at 是 UNIX timestamp，所以你可以用它来精确过滤“近 1 小时”（比如 created_at >= now - 3600）。
但文档没有说明单次请求返回的 total_count 是否 100% 实时准确，也没有说明 tag_ids 过滤的延迟情况。
速率限制：文档里没有提到 rate limit 数值。
去重机制：文档里没有提供任何状态或增量变更的字段，所以“避免重复告警”只能靠你自己的状态管理（比如记录已告警的时间窗口）。

我觉得按照 2min 轮询 1 次就可以。

Xiaoyu，Intercom REST API 的默认速率限制是：每个应用（app）每分钟最多 10,000 次 API 调用，每个工作区（workspace）每分钟最多 25,000 次 API 调用。
如果你超过限制，API 会返回 HTTP 429（Too Many Requests）。
另外，API 响应里会带这几个限流头，方便你控制轮询节奏：
X-RateLimit-Limit
X-RateLimit-Remaining
X-RateLimit-Reset（重置时间，UNIX 时间戳）

API 的 rate limit 是多少 QPS 100 吧

Xiaoyu，会话 tag 的 ID 就是 tag 的唯一标识符（tag_id），它是一个字符串。
你可以用这个 API 先通过 tag_id 查到 tag 的详情：
查找特定的 tag（传入 tag_id，返回 tag 对象）。
所以，如果你已经知道 tag_id，就可以直接用它来过滤会话；如果还不知道，先用上面这个接口把 tag 查出来，就能拿到它的 ID。